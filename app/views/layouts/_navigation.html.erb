<%= link_to "Home", root_path, :class => 'brand' %>
<ul class="nav">
  <% if current_page? (doe_path) %>
	<li class="active">
  <% else %>
  	<li>
  <% end %>
		<%= link_to 'Doe', doe_path %>
	</li>
	
  <% if current_page? (haas_path) %>
	<li class="active">
  <% else %>
  	<li>
  <% end %>
		<%= link_to 'Haas', haas_path %>
	</li>
	
  <% if current_page? (hangrove_path) %>
	<li class="active">
  <% else %>
  	<li>
  <% end %>
		<%= link_to 'Hangrove', hangrove_path %>
	</li>
	
  <% if current_page? (kresge_path) %>
	<li class="active">
  <% else %>
  	<li>
  <% end %>
		<%= link_to 'Kresge', kresge_path %>
	</li>
	
  <% if current_page? (moffitt_path) %>
	<li class="active">
  <% else %>
  	<li>
  <% end %>
		<%= link_to 'Moffitt', moffitt_path %>
	</li>
	
  <% if current_page? (vlsb_path) %>
	<li class="active">
  <% else %>
  	<li>
  <% end %>
		<%= link_to 'VLSB', vlsb_path %>
	</li>
	
  <% if current_page? (wurster_path) %>
	<li class="active">
  <% else %>
  	<li>
  <% end %>
		<%= link_to 'Wurster', wurster_path %>
	</li>
</ul>